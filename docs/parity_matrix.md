| Milestone | Status | Evidence |
| --- | --- | --- |
| M1 inventory + codegen | Implemented | Route, config, database, and module inventories are present and registered. |
| M2 core runtime/CLI/config parity | Implemented | Config defaults and CLI compatibility tests cover the generated config model. |
| M3 database compatibility | Implemented | In-memory database runtime, schema, serialization, map clear, key iteration, bounded scans, prefix scans, reverse scans, and RocksDB-disabled policy tests pass. |
| M4+ | In progress | Native runtime smoke paths pass and client appstate/device/ephemeral/presence/profile/room-history/search/push/key-backup/E2EE-key/cross-signing/to-device/dehydrated-device routes now persist filters, account data, tags, device metadata, uploaded device keys, one-time keys, fallback keys, cross-signing keys and signatures, per-device to-device sync queues, dehydrated devices, pushers, push rules, room key backup versions and sessions, report records, read receipts, fully-read markers, local presence, profile fields, aliases, member-event filters, timeline context/event pagination, joined-room search, notification polling, deprecated event polling, relations, thread roots, mutual-room lookup, initial-sync and summary room state, empty-key room-state aliases, sliding sync v5 room windows, space hierarchy, room upgrades, TURN server config, third-party empty responses, joined-room lists, public-room directory state, alias state, room visibility, membership actions, user-directory search, OpenID token payloads, SSO redirects/callback/login-token completion, OIDC next-gen auth registration/authorize/complete/token/userinfo/revoke state and account-management profile/session callbacks, client login-token/refresh-token/password/deactivation and registration-token checks, client media upload/config/preview/download/thumbnail routes, local storage-provider object operations, provider chunking, storage admin query operations, well-known discovery, server key publication, local user counts, federation read APIs for version, public rooms, event/state/backfill/auth-chain, directory/profile, devices, keys, hierarchy, OpenID userinfo, and authenticated media, and federation transaction/membership ingress for send, join, leave, knock, invite, configurable typing expiry, presence activity metadata, read-receipt, direct-to-device, signing-key-update, and device-list-update EDUs, plus appservice namespace-filtered PDU delivery and receive_ephemeral typing/receipt EDU delivery. No tracked client compatibility shell or fallback routes remain, but deeper federation/appservice semantics, native S3 storage, and full appservice semantics remain before full Rust parity. |
