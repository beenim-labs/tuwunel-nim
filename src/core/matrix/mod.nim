const
  RustPath* = "core/matrix/mod.rs"
  RustCrate* = "core"
  GeneratedAt* = "2026-05-12T00:00:00+00:00"

import core/matrix/[event, pdu, room_version, server_signing]

export event, pdu, room_version, server_signing
