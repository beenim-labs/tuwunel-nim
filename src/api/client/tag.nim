## client/tag — api module.
##
## Ported from Rust api/client/tag.rs

import std/[options, json, tables, strutils]

const
  RustPath* = "api/client/tag.rs"
  RustCrate* = "api"

proc updateTagRoute*() =
  ## Ported from `update_tag_route`.
  discard

proc deleteTagRoute*() =
  ## Ported from `delete_tag_route`.
  discard

proc getTagsRoute*() =
  ## Ported from `get_tags_route`.
  discard
