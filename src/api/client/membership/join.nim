## membership/join — api module.
##
## Ported from Rust api/client/membership/join.rs

import std/[options, json, tables, strutils]

const
  RustPath* = "api/client/membership/join.rs"
  RustCrate* = "api"

proc joinRoomByIdRoute*() =
  ## Ported from `join_room_by_id_route`.
  discard

proc joinRoomByIdOrAliasRoute*() =
  ## Ported from `join_room_by_id_or_alias_route`.
  discard
