## client/filter — api module.
##
## Ported from Rust api/client/filter.rs

import std/[options, json, tables, strutils]

const
  RustPath* = "api/client/filter.rs"
  RustCrate* = "api"

proc getFilterRoute*() =
  ## Ported from `get_filter_route`.
  discard

proc createFilterRoute*() =
  ## Ported from `create_filter_route`.
  discard
