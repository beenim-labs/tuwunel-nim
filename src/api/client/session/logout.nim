## session/logout — api module.
##
## Ported from Rust api/client/session/logout.rs

import std/[options, json, tables, strutils]

const
  RustPath* = "api/client/session/logout.rs"
  RustCrate* = "api"

proc logoutRoute*() =
  ## Ported from `logout_route`.
  discard

proc logoutAllRoute*() =
  ## Ported from `logout_all_route`.
  discard
