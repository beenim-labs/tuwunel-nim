## session/mod — api module.
##
## Ported from Rust api/client/session/mod.rs

import std/[options, json, tables, strutils]

const
  RustPath* = "api/client/session/mod.rs"
  RustCrate* = "api"

proc getLoginTypesRoute*() =
  ## Ported from `get_login_types_route`.
  discard

proc loginRoute*() =
  ## Ported from `login_route`.
  discard
